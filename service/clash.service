[Unit]
Description=Clash Proxy
After=network.target

[Service]
Type=simple
User=cai
Group=cai
WorkingDirectory=%h/.clash
ExecStart=%h/.local/bin/clash
ExecReload=/bin/kill -HUP $MAINPID
ExecStop=/bin/kill -TERM $MAINPID
Restart=on-failure
RestartSec=5

[Install]
WantedBy=default.target
